# 国服／Wiki 权威来源穷尽审计（只读）

## 可直接采用

共 **275** 个字段／记录：官方帮助 229、Wiki 少女史记 12、同路径官方模板 13、官方 Raid 台词 21。帮助必须按 `type + id + field` 合并；上游新增 22 条、49 个字段无旧国服对应。`EventRaidMessage.json` 被当前 `EventRaidTop.js` 精确依赖，旧国服 21 条结构可恢复。另有 14 个旧国服模板须先验证 EJS/选择器合同，不计入直接采用数。

## 仍需人工翻译／审查

共 **124** 个字段：帮助 49、最终决战 16、贴纸 8、CSS 10、Totentanz 公告 18、活动横幅 23。公告与横幅不得按旧数组位置回收。

## 928 个旧国服独有路径

实测 **928**：JSON 456、JS 203、HTML 136、CSS 133。其中 `api/`/`json/` 生成传输快照 447 个；CSS 无直接文本 103 个、有可见 `content:` 9 个。

## 安全

产品写入 0、GitHub 写入 0。禁止对新增文件运行无上下文全局映射，必须稳定键／路径限定。
