# 第三轮可见文本权威化

本轮只处理已经确认会显示在游戏界面中的字面文本：

- 49 处采用旧国服同功能、同路径或同字段证据；
- 17 处无旧国服中文可直接回收的活动按钮，由本轮人工译为“确定”；
- 所有替换均按文件和完整字面值限定，不修改服务器键、枚举、HTML 属性或代码结构；
- `apply-visible-ui-localization.py` 负责写入、状态验证和事务回滚。

来源优先级保持为：旧国服官方文本 > Wiki > 已确认人工译文 > 新人工译文。
