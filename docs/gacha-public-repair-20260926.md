# 抽卡修复进入正式公共更新链路

## 断点

2026-08-23 的 `71008d82` 流水线只上传存活 1 天的临时审计附件，未更新 `latest` Release 或版本门控。2026-09-26 对公开 `cn_base_02.zip` 重新按范围读取并验 CRC、SHA256：基础包仍为 `310495d7...`，45 项场景静态资源中有 20 项不同于用户确认正常的设备-3。累计包 v12 没有这些场景路径。故旧设备的本地注入从未形成公开修复，不是设备兼容差异。

## 本次交付

- 将设备-3只读快照的 45 个 `scene/gacha_v2` 静态资源原样进入产品树及累计补充包；包括动画、图集、9 组特效及其配套纹理。20 项替换旧内容，其余25项保持配套一致。没有重新生成图像或特效。
- 排除抽卡结果 `down_png/`、`debug/` 和不完整的旧 `scene/gacha/` 路径，避免复制设备缓存或无关覆盖。
- Connecting 按用户新要求恢复既有 8 帧循环 APNG。此项为此前国服静态图优先规则的明确例外；其余21项国服原图和14项已验收原图不变。
- 发布前强制校验45项哈希及场景依赖。增量包累计携带历史更改，旧用户直接升级和全新安装均须经同一正式下载链路；不以向单台设备写入替代发布。

源码／包校验与游戏内用户验收分别记录，发布不自动代表后者通过。
